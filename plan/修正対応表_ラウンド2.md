# 修正対応表 — ラウンド2

## P0（必須）対応状況

| ID | 指摘内容 | 対応内容 | 状態 |
|----|---------|---------|------|
| CRI-01 | マネジメントレビューモジュール（M-11）の完全欠落 | M-11として独立セクション（セクション8）を新規追加。目的、JIS Q 15001 J.4.6.3対応、ユーザーストーリー5件、画面設計6画面、データモデル（ManagementReview, ManagementReviewAgenda, ManagementReviewDecision）、API設計14エンドポイント、AI連携4機能、ワークフロー（DRAFT→PREPARED→IN_REVIEW→APPROVED→LOCKED）、権限設計を完全定義。M-09との連携（レビューサマリーを申請パッケージに含める）も具体化。年次運用サイクル表にマネジメントレビューを反映。 | 完了 |
| CRI-02 | 全新規モデルにOrganizationへの双方向リレーション欠落 | 全新規モデル（RiskAssessment, PMSDocument, TrainingPlan, Vendor, AuditPlan, IncidentCase, ApplicationPackage, ManagementReview等）に`organization Organization @relation(fields: [organizationId], references: [id])`を追加。Organizationモデルへの逆リレーション配列を「既存モデルへの変更一覧」セクションに一覧化。全モデルに`@@index([organizationId])`を追加。 | 完了 |
| CRI-03 | EvidenceRecord拡張で逆リレーション未定義 | 新規モデル側（RiskItem, Vendor, IncidentCase, AuditFinding, CorrectiveAction）に`evidenceRecords EvidenceRecord[]`を追加。EvidenceRecord側に各`@relation`定義を追加。APIに「少なくとも1つの紐付け先が必須」のバリデーションルールを明記。 | 完了 |
| CRI-04 | RiskItem→RegisterItem/BusinessProcessの双方向リレーション不備 | RiskItemに`registerItem RegisterItem? @relation(fields: [registerItemId], references: [id])`および`businessProcess BusinessProcess? @relation(fields: [businessProcessId], references: [id])`を追加。既存モデル変更としてRegisterItem側に`riskItems RiskItem[]`、BusinessProcess側に`riskItems RiskItem[]`を明記。RiskAssessmentに`targetScope`フィールドを追加。 | 完了 |
| CRI-05 | CorrectiveAction→IncidentCaseの双方向リレーション不整合 | CorrectiveActionに`incident IncidentCase? @relation(fields: [incidentId], references: [id])`を追加。IncidentCase側に`correctiveActions CorrectiveAction[]`を追加。 | 完了 |

## P1（強く推奨）対応状況

| ID | 指摘内容 | 対応内容 | 状態 |
|----|---------|---------|------|
| MED-01 | AUDITORロールの扱い不明確 | AUDITORは「監査責任者/主任監査員」ロールとして定義。個別計画の監査員はAuditPlanAuditor中間テーブルで管理。全モジュール権限表にAUDITORとGLOBAL_ADMINの行を追加。 | 完了 |
| MED-02 | JSON型フィールドの多用 | AuditPlan.targetDeptIds→AuditPlanDepartment中間テーブル化、AuditPlan.auditorIds→AuditPlanAuditor中間テーブル化。他のJSON型（scores, options等）は表示専用のためJSON維持とし、TypeScript型定義を明記。 | 完了 |
| MED-03 | fiscalYear型の未統一 | 全モデルでfiscalYearをInt型に統一（例: 2025）。横断仕様として定義。 | 完了 |
| MED-04 | 本人対応・苦情相談管理が未対象 | 「対象外業務領域」セクションを追加。MVPスコープ外とする判断と理由を明記。将来対応として言及。 | 完了 |
| MED-05 | 変更管理（ChangeRequest）が未対象 | 同上。MVPスコープ外とする判断と理由を明記。M-04文書管理の変更履歴機能で最低限の変更記録は対応可能である旨を記載。 | 完了 |
| MED-06 | PUTの部分更新仕様が未定義 | 横断仕様として「PUTは部分更新を許容する（送信されたフィールドのみ更新）」を明記。 | 完了 |
| MED-07 | User参照フィールドの@relation未定義 | 全User参照フィールドに`@relation(name: "xxx")`を追加。User側の逆リレーションも既存モデル変更一覧に列挙。 | 完了 |
| MED-08 | 第三者提供管理の位置づけ不明確 | 対象外業務領域セクションで、M-06委託先管理の拡張として将来対応する旨を明記。 | 完了 |

## P2（推奨）対応状況

| ID | 指摘内容 | 対応内容 | 状態 |
|----|---------|---------|------|
| MIN-04 | AiUsageLog.feature追加値が未列挙 | 各モジュールのAI連携セクションに対応するfeature値を明記。 | 完了 |
| 横断 | インデックス設計の不在 | 全新規モデルに@@index定義を追加（organizationId, fiscalYear, status等）。 | 完了 |
| 横断 | 既存モデルへの変更一覧の不在 | 「既存モデルへの変更一覧（Migration Impact List）」セクションを新設。Organization, User, RegisterItem, BusinessProcess, EvidenceRecordへの変更を一覧化。 | 完了 |
| 横断 | organizationIdスコーピング横断仕様の不在 | 横断API仕様セクションに明記。 | 完了 |
